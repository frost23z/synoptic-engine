/** Shared UI constants. Auto-imported by Nuxt (`app/utils/*`). */

/** Default page size for paginated list views. */
export const PAGE_SIZE = 20

/** Default debounce (ms) for search inputs. */
export const SEARCH_DEBOUNCE_MS = 300
