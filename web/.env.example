# Frontend runtime configuration.
# Base URL of the Synoptic Engine API (host root — all endpoints live under /api/*).
# Required in production; in local dev it defaults to http://localhost:8090 (see
# app/plugins/api-base.ts). Copy to `.env` for local development.
NUXT_PUBLIC_API_BASE=http://localhost:8090
